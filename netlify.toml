[build]
  command = "npm install && npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "20.18.1"

# Run build daily to refresh events
[build.processing]
  skip_processing = false
